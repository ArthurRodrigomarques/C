#include <stdio.h>

int main () {
    int n=0, x=0, i;

    for(i=0; i<=10; i++) {
       // n = i;
    printf("n = %d\n", n);
    };
    printf("i = %d\n\n", i);

    // n = 0;
    // n = 1;
    // n = 2;
    // x = 1;

    x++;

    printf("Hello world!\n");
    return 0;
}